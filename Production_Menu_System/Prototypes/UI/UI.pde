class UI implements Drawable {
	private List<Integer> chapters;
	
	public UI(){
		
	}
	
	void draw(){
		Chapter chapter = new Chapter();
		chapter.draw();
		// draw the start page (probably SystemStatus)
	}
} [1, 2, 3, 4, 5]; 2, [6, 7, 8, 9]; 3, [10, 11, 12, 13, 14]);
			// Initialize status variables
	}
	
	void draw(){
		Chapter chapter = new Chapter();
		chapter.draw();
		// draw the start page (probably SystemStatus)
	}
}	}
}