class Page implements Drawable {
	private List<String> pages = ["Calculator", "Map", "System", "Clock", etc.];
	
	public Page(int pageNum){
		switch (pageNum):
			//set up the page
	}
	
	void draw(){
		switch (pageNum): 
		// draw the elements for this specific page
	}
}